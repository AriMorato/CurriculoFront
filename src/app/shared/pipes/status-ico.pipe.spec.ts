import { StatusIcoPipe } from './status-ico.pipe';

describe('StatusIcoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusIcoPipe();
    expect(pipe).toBeTruthy();
  });
});
